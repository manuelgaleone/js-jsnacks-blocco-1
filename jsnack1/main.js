//Chiedo due numeri all'Utente
let userNumber = prompt("Dammi un Numero:");
let secondUserNumber = prompt("Dammi un altro Numero:");

//Calcolo quale sia il più grande tra i due
if (userNumber > secondUserNumber) {

    alert("Il numero Maggiore è: " + userNumber);

} else {
    
    alert("Il numero Maggiore è: " + secondUserNumber);

};